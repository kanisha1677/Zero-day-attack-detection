# Zero-day-attack-detection
Traditional signature-based Intrusion Detection Systems (IDS) struggle to identify these unknown attacks. This creates a need for an adaptive detection system that can identify abnormal network behaviour without relying only on known attack patterns. The system should also reduce false alarms and provide understandable reasons for its predictions.
